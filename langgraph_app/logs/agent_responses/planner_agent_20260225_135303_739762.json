{
    "raw_agent_response": "{\n    \"plan\": [\n        {\n            \"visualization\": true,\n            \"sql_required\": true,\n            \"task\": \"Return one row per order containing: order_id, customer_id, seller_id, customer_zip_code_prefix, seller_zip_code_prefix, customer_latitude, customer_longitude, seller_latitude, seller_longitude, distance_km (great-circle distance between seller and customer computed from their geolocation coordinates), order_purchase_timestamp, order_delivered_timestamp, and delivery_time_days (difference in days between order_delivered_timestamp and order_purchase_timestamp). Include only orders with non-null order_delivered_timestamp and non-null coordinates for both seller and customer. Use these joins: orders.customer_id = customers.customer_id; orders.order_id = order_items.order_id; order_items.seller_id = sellers.seller_id; customers.customer_zip_code_prefix = geolocation.zip_code_prefix (alias customer_geo); sellers.seller_zip_code_prefix = geolocation.zip_code_prefix (alias seller_geo). Do not aggregate — return a flat dataset (one row per order).\"\n        }\n    ]\n}"
}