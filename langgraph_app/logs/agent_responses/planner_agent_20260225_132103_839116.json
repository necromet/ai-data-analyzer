{
    "raw_agent_response": "{\n    \"plan\": [\n        {\n            \"visualization\": true,\n            \"sql_required\": true,\n            \"task\": \"For each product category (use English category names: products.product_category_name_english), produce one row with these numeric aggregates: avg_price = AVG(order_items.price), avg_freight_value = AVG(order_items.freight_value), avg_payment_value = AVG(order_payments.payment_value), avg_review_score = AVG(order_reviews.review_score), total_revenue = SUM(order_items.price * order_items.order_item_quantity), total_units_sold = SUM(order_items.order_item_quantity), order_count = COUNT(DISTINCT orders.order_id). Join tables using the allowed relationships (order_items -> orders -> order_payments and order_reviews; order_items -> products). Include only categories with order_count >= 30. Return a flat table (one row per category) suitable for plotting a heatmap where columns are the numeric metrics above.\"\n        }\n    ]\n}"
}