{
    "raw_agent_response": "{\n    \"plan\": [\n        {\n            \"visualization\": true,\n            \"sql_required\": true,\n            \"task\": \"For each order item, return seller_state, customer_state, and delivery_time_days (difference in days between orders.order_delivered_customer_date and orders.order_purchase_timestamp). Join order_items to orders (orders.order_id = order_items.order_id), join order_items to sellers (order_items.seller_id = sellers.seller_id) and sellers to geolocation (sellers.seller_zip_code_prefix = geolocation.zip_code_prefix) to get seller_state, and join customers to geolocation (customers.customer_zip_code_prefix = geolocation.zip_code_prefix) and orders to customers (orders.customer_id = customers.customer_id) to get customer_state. Include only rows where orders.order_purchase_timestamp and orders.order_delivered_customer_date are not null. Return one flat table with columns: seller_state, customer_state, delivery_time_days.\"\n        }\n    ]\n}"
}