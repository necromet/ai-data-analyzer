{
    "raw_agent_response": "{\n    \"plan\": [\n        {\n            \"visualization\": true,\n            \"sql_required\": true,\n            \"task\": \"For each combination of customer state and seller state, compute the average delivery delay in days and the number of orders, then return the top 30 combinations by highest average delivery delay. Define delivery delay as the difference in days between orders.order_delivered_customer_date and orders.order_estimated_delivery_date. Exclude rows where either date is NULL. Join tables using: orders.order_id = order_items.order_id and order_items.seller_id = sellers.seller_id and orders.customer_id = customers.customer_id and customers.customer_zip_code_prefix = geolocation.zip_code_prefix and sellers.seller_zip_code_prefix = geolocation.zip_code_prefix. Return a flat table with columns: customer_state, seller_state, average_delivery_delay_days, order_count, ordered by average_delivery_delay_days DESC, limited to 30 rows.\"\n        }\n    ]\n}"
}